const capitalzedString = string => {
  return string.charAt[0].toUpperCase() + string.slice(1);
};

export { capitalizedString };

export const foo = "bar";
export const bar = "foo";

export default function subtract(x, y) {
  return x - y;
}
